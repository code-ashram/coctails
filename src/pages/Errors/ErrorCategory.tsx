import styles from '../../Root.module.css'

const ErrorCategory = () => (
  <section className={styles.errorPage}>
    <h2 className={styles.errorTitle}>No such drinks =(</h2>
  </section>
)

export default ErrorCategory
